<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %></title>

  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Font Awesome (Updated to Kit or CDN CSS for better performance) -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" integrity="sha512-papvvm13uVnDz8q1E/Cz9QZK9d0cZ4ex+SjaWd7i/BVZmtv68f4uY25Ggf96YJz4p6A+qEfq/UlEz8vB2n7z7Q==" crossorigin="anonymous" referrerpolicy="no-referrer" />

  <style>
    body {
      background: linear-gradient(to bottom right, #c7d2fe, #e9d5ff, #fbcfe8);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .glass-card {
      background: rgba(255, 255, 255, 0.3);
      backdrop-filter: blur(10px);
      border-radius: 2rem;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
      padding: 50px;
      width: 100%;
      max-width: 700px;
      text-align: center;
    }

    .btn-custom {
      padding: 14px 28px;
      font-size: 1.1rem;
      border-radius: 50px;
      font-weight: 600;
    }

    @media (max-width: 576px) {
      .glass-card {
        padding: 30px;
      }
      h1 {
        font-size: 1.6rem;
      }
      .btn-custom {
        padding: 12px 20px;
        font-size: 1rem;
      }
    }
  </style>
</head>
<body>

  <div class="container-fluid d-flex align-items-center justify-content-center min-vh-100">
    <div class="glass-card">

      <h1 class="mb-5 fw-bold text-dark fs-2"><%= title %></h1>

      <form id="uploadForm" action="/upload" method="post" enctype="multipart/form-data" novalidate>
        <div class="mb-4 text-start">
          <label for="f1" class="form-label fw-semibold text-dark">Upload File</label>
          <input type="file" name="file" id="f1" class="form-control form-control-lg rounded-pill" accept=".csv" required>
          <div class="form-text">Only .csv files are allowed.</div>
        </div>

        <button type="submit" class="btn btn-primary btn-custom w-100">
          <i class="fas fa-upload me-2" aria-hidden="true"></i> Upload
        </button>
      </form>

      <div class="mt-4">
        <a href="/" class="btn btn-light btn-custom">
          <i class="fas fa-home me-2" aria-hidden="true"></i> Home
        </a>
      </div>

    </div>
  </div>

  <!-- Bootstrap 5 JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
