<!DOCTYPE html>
<html>
<head>
  <title><%= username %>'s Profile</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
    }
    .profile-header {
      background: linear-gradient(to right, #007bff, #6610f2);
      color: white;
      padding: 30px;
      border-radius: 12px;
      text-align: center;
    }
    .username-link {
      color: #fff;
      text-decoration: none;
      font-size: 2rem;
      font-weight: bold;
      transition: color 0.3s ease;
    }
    .username-link:hover {
      color: #ffd700;
    }
    .card-img-top {
      object-fit: cover;
      height: 250px;
    }
    .stats-badge {
      font-size: 0.9rem;
      margin-right: 10px;
    }
  </style>
</head>
<body class="bg-light">
  <div class="container mt-5">
    <!-- Profile Header -->
    <div class="profile-header mb-4">
      <h2>
        <a href="https://instagram.com/<%= username %>" target="_blank" class="username-link">
          <i class="fab fa-instagram me-2"></i>@<%= username %>
        </a>
      </h2>
      <p class="mb-0">
        <span class="badge bg-warning text-dark stats-badge">Followers: <%= followers %></span>
        <span class="badge bg-info text-dark stats-badge">Posts: <%= mediaCount %></span>
        <span class="badge bg-light text-dark stats-badge">Engagement Rate: <%= engagement %>%</span>
      </p>
    </div>

    <!-- Media Cards -->
    <div class="row row-cols-1 row-cols-md-3 g-4">
      <% recentMedia.slice(0, 10).forEach(media => { %>
      <div class="col">
        <div class="card h-100 shadow-sm">
          <% if(media.media_url) { %>
            <img src="<%= media.media_url %>" class="card-img-top" alt="Media" />
            
          <% } %>
          <div class="card-body">
            <p class="card-text">
              <%= media.caption ? media.caption.slice(0, 100) + '...' : 'No caption' %>
            </p>
          </div>
          <div class="card-footer d-flex justify-content-between small text-muted">
            <span>❤️ <%= media.like_count %></span>
            <span>💬 <%= media.comments_count %></span>
            <span>👁️ <%= media.view_count || 0 %></span>
          </div>
        </div>
      </div>
      <% }); %>
    </div>

    <!-- Back Button -->
    <div class="text-center mt-5">
      <a href="/feature/celebrities" class="btn btn-outline-dark">🔍 Search Another</a>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
